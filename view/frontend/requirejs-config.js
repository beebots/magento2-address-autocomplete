var config = {
    map: {
        '*': {
            'checkout-address-autocomplete': 'BeeBots_AddressAutocomplete/js/checkout-address-autocomplete'
        }
    },
    deps: [
        'jquery'
    ]
};
